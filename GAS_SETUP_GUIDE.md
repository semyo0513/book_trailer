# 🚀 Google Apps Script(GAS) 및 구글 시트 연동 가이드

희망도서 신청 웹앱을 실제 구글 시트 및 알라딘 Open API와 연동하기 위한 단계별 안내 매뉴얼입니다.

---

## 1. 구글 스프레드시트 생성

1. [Google Drive](https://drive.google.com)에 접속하여 **새 스프레드시트**를 만듭니다.
2. 스프레드시트 제목을 `희망도서 신청 관리` 등으로 변경합니다.
3. 시트에 두 개의 탭이 자동으로 생성됩니다:
   - **`희망도서신청` 탭**: 사용자가 신청한 도서 내역 데이터베이스
   - **`환경설정` 탭**: 웹앱 제목, 부제목, 공지 문구, 권수/금액 제한, 관리자 비밀번호 등 설정값 저장소

---

## 2. Google Apps Script(GAS) 코드 등록 및 업데이트

1. 구글 시트 상단 메뉴에서 **확장 프로그램 &rarr; Apps Script**를 클릭합니다.
2. 기존 `Code.gs`의 내용을 모두 지웁니다.
3. 프로젝트의 `gas/Code.gs` 파일 내용을 복사하여 붙여넣고 저장(`Ctrl + S`)합니다.

---

## 3. 알라딘 TTB 키 설정 (선택 사항)

알라딘 도서 검색 API를 통해 실시간 검색 및 ISBN 자동 조회를 사용하려면 TTB 키를 등록합니다.

1. 알라딘 Open API 사이트 또는 알라딘 계정에서 **TTBKey(API Key)**를 발급받습니다.
2. Apps Script 편집기 좌측 메뉴에서 **프로젝트 설정 (톱니바퀴 아이콘 ⚙️)**을 클릭합니다.
3. 하단의 **스크립트 속성(Script Properties)** 항목에서 **스크립트 속성 추가**를 클릭합니다:
   - **속성(Property)**: `ALADIN_TTB_KEY`
   - **값(Value)**: 발급받은 TTB 키 (예: `ttb...`)
4. **스크립트 속성 저장**을 클릭합니다.

> 💡 **참고**: TTB 키를 등록하지 않아도 웹앱의 시뮬레이션(Mock) 모드로 UI 및 저장 흐름 테스트가 가능합니다.

---

## 4. 웹 앱(Web App) 신규 배포 또는 버전 업데이트

### A. 처음 배포하는 경우
1. Apps Script 편집기 우측 상단의 파란색 **배포 &rarr; 새 배포**를 클릭합니다.
2. 유형으로 **웹 앱**을 선택합니다.
3. 배포 설정을 다음과 같이 구성합니다:
   - **설명**: `희망도서 웹앱 v2 (환경설정 시트 연동)`
   - **다음 사용자로 실행 (Execute as)**: `나(본인 이메일 주소)` (중요!)
   - **액세스 권한이 있는 사용자 (Who has access)**: `모든 사용자 (Anyone)` (중요!)
4. **배포** 버튼을 클릭하고 생성된 **웹 앱 URL**을 복사합니다.

### B. 이미 배포된 상태에서 코드를 수정한 경우 (중요!)
1. Apps Script 편집기 우측 상단의 **배포 &rarr; 배포 관리**를 클릭합니다.
2. 활성 배포 항목 우측의 **수정(연필 아이콘 ✏️)**을 클릭합니다.
3. **버전(Version)** 드롭다운을 눌러 **[새 버전]**을 선택합니다.
4. 하단의 **배포** 버튼을 클릭하면 기존 웹 앱 URL 그대로 새 코드가 적용됩니다!

---

## 5. 구글 시트 시트별 컬럼 구성

### ① `희망도서신청` 탭 (신청 데이터)
| 열 | 컬럼명 | 설명 |
|:---|:---|:---|
| **A** | 신청일시 | 서버 타임스탬프 (YYYY-MM-DD HH:mm:ss) |
| **B** | 신청자명 | 사용자 입력 신청자 이름 |
| **C** | 도서명 | 신청 도서 제목 |
| **D** | 저자명 | 저자 및 역자 |
| **E** | 출판사 | 출판사 |
| **F** | 가격 | 정가 / 판매가 |
| **G** | 구매처 | 알라딘, 교보문고, YES24 등 |
| **H** | 구매링크 | 도서 상세 페이지 URL |
| **I** | 상태 | 기본값 `신청접수` (관리자 변경용) |
| **J** | 비고 | 신청 사유 / 전달사항 |

### ② `환경설정` 탭 (관리자 설정값 영구 보관)
| 열 | 항목 | 설명 |
|:---|:---|:---|
| **A** | 설정 키 (Key) | `appTitle`, `appSubtitle`, `noticeText`, `monthlyLimit`, `priceLimit`, `sheetUrl`, `successMessage`, `adminPassword` |
| **B** | 설정 값 (Value) | 실제 설정된 텍스트/숫자 값 |
| **C** | 설명 (Description) | 설정 항목에 대한 한글 설명 |
